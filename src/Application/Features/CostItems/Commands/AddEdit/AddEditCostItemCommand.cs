//------------------------------------------------------------------------------
// <auto-generated>
// CleanArchitecture.Blazor - MIT Licensed.
// Author: neozhu
// Created/Modified: 2025-09-24
// Command for adding/editing a costitem entity with validation, mapping,
// domain events, and cache invalidation.
// Documentation: https://docs.cleanarchitectureblazor.com/features/costitem
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
#nullable disable warnings


using CleanArchitecture.Blazor.Application.Features.CostItems.Caching;
using CleanArchitecture.Blazor.Application.Features.CostItems.DTOs;
namespace CleanArchitecture.Blazor.Application.Features.CostItems.Commands.AddEdit;

public class AddEditCostItemCommand: ICacheInvalidatorRequest<Result<int>>
{
      [Description("Id")]
      public int Id { get; set; }
          [Description("Project id")]
    public int ProjectId {get;set;} 
    [Description("Project")]
    public ProjectDto? Project {get;set;} 
    [Description("Vendor id")]
    public int? VendorId {get;set;} 
    [Description("Vendor")]
    public VendorDto? Vendor {get;set;} 
    [Description("Description")]
    public string? Description {get;set;} 
    [Description("Quantity")]
    public decimal Quantity {get;set;} 
    [Description("Unit cost")]
    public decimal UnitCost {get;set;} 
    [Description("Total cost")]
    public decimal TotalCost {get;set;} 
    [Description("Category")]
    public string? Category {get;set;} 


      public string CacheKey => CostItemCacheKey.GetAllCacheKey;
      public IEnumerable<string>? Tags => CostItemCacheKey.Tags;
    private class Mapping : Profile
    {
        public Mapping()
        {
            CreateMap<CostItemDto, AddEditCostItemCommand>(MemberList.None);
            CreateMap<AddEditCostItemCommand, CostItem>(MemberList.None);
        }
    }
}

public class AddEditCostItemCommandHandler : IRequestHandler<AddEditCostItemCommand, Result<int>>
{
    private readonly IMapper _mapper;
    private readonly IApplicationDbContext _context;
    public AddEditCostItemCommandHandler(
        IMapper mapper,
        IApplicationDbContext context)
    {
        _mapper = mapper;
        _context = context;
    }
    public async Task<Result<int>> Handle(AddEditCostItemCommand request, CancellationToken cancellationToken)
    {
        if (request.Id > 0)
        {
            var item = await _context.CostItems.FindAsync(request.Id, cancellationToken);
            if (item == null)
            {
                return await Result<int>.FailureAsync($"CostItem with id: [{request.Id}] not found.");
            }
            item = _mapper.Map(request, item);
			// raise a update domain event
			item.AddDomainEvent(new CostItemUpdatedEvent(item));
            await _context.SaveChangesAsync(cancellationToken);
            return await Result<int>.SuccessAsync(item.Id);
        }
        else
        {
            var item = _mapper.Map<CostItem>(request);
            // raise a create domain event
			item.AddDomainEvent(new CostItemCreatedEvent(item));
            _context.CostItems.Add(item);
            await _context.SaveChangesAsync(cancellationToken);
            return await Result<int>.SuccessAsync(item.Id);
        }
       
    }
}

