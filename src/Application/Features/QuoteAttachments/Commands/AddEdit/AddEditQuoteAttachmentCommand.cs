//------------------------------------------------------------------------------
// <auto-generated>
// CleanArchitecture.Blazor - MIT Licensed.
// Author: neozhu
// Created/Modified: 2025-09-24
// Command for adding/editing a quoteattachment entity with validation, mapping,
// domain events, and cache invalidation.
// Documentation: https://docs.cleanarchitectureblazor.com/features/quoteattachment
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
#nullable disable warnings


using CleanArchitecture.Blazor.Application.Features.QuoteAttachments.Caching;
using CleanArchitecture.Blazor.Application.Features.QuoteAttachments.DTOs;
namespace CleanArchitecture.Blazor.Application.Features.QuoteAttachments.Commands.AddEdit;

public class AddEditQuoteAttachmentCommand: ICacheInvalidatorRequest<Result<int>>
{
      [Description("Id")]
      public int Id { get; set; }
          [Description("Quote id")]
    public int QuoteId {get;set;} 
    [Description("Quote")]
    public QuoteDto? Quote {get;set;} 
    [Description("File name")]
    public string? FileName {get;set;} 
    [Description("File path")]
    public string? FilePath {get;set;} 
    [Description("Uploaded at utc")]
    public DateTime? UploadedAtUtc {get;set;} 


      public string CacheKey => QuoteAttachmentCacheKey.GetAllCacheKey;
      public IEnumerable<string>? Tags => QuoteAttachmentCacheKey.Tags;
    private class Mapping : Profile
    {
        public Mapping()
        {
            CreateMap<QuoteAttachmentDto, AddEditQuoteAttachmentCommand>(MemberList.None);
            CreateMap<AddEditQuoteAttachmentCommand, QuoteAttachment>(MemberList.None);
        }
    }
}

public class AddEditQuoteAttachmentCommandHandler : IRequestHandler<AddEditQuoteAttachmentCommand, Result<int>>
{
    private readonly IMapper _mapper;
    private readonly IApplicationDbContextFactory _dbContextFactory;;
    public AddEditQuoteAttachmentCommandHandler(
        IMapper mapper,
        IApplicationDbContext context)
    {
        _mapper = mapper;
        _context = context;
    }
    public async Task<Result<int>> Handle(AddEditQuoteAttachmentCommand request, CancellationToken cancellationToken)
    {
        if (request.Id > 0)
        {
            var item = await _context.QuoteAttachments.FindAsync(request.Id, cancellationToken);
            if (item == null)
            {
                return await Result<int>.FailureAsync($"QuoteAttachment with id: [{request.Id}] not found.");
            }
            item = _mapper.Map(request, item);
			// raise a update domain event
			item.AddDomainEvent(new QuoteAttachmentUpdatedEvent(item));
            await _context.SaveChangesAsync(cancellationToken);
            return await Result<int>.SuccessAsync(item.Id);
        }
        else
        {
            var item = _mapper.Map<QuoteAttachment>(request);
            // raise a create domain event
			item.AddDomainEvent(new QuoteAttachmentCreatedEvent(item));
            _context.QuoteAttachments.Add(item);
            await _context.SaveChangesAsync(cancellationToken);
            return await Result<int>.SuccessAsync(item.Id);
        }
       
    }
}

